package main

import (
    "errors"
	"github.com/aws/aws-lambda-go/lambda"
	lib "github.com/Astenna/Nubes/lib"
    {{.TypesPackageAlias}} "{{.TypesPackagePath}}"
)

func Get{{.TypeName}}Handler(id string) ({{.TypesPackageAlias}}.{{.TypeName}}, error) {
    output, err := lib.Get[{{.TypesPackageAlias}}.{{.TypeName}}](id)

    if err != nil {
        return *output, err
    }
    return *output, nil
}

func main() {
	lambda.Start(Get{{.TypeName}}Handler)
}
